[project]
name = "support-agent-common"
version = "0.1.0"
description = "Shared domain code for the support-agent demo variants"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
